import { useEffect, useRef, useState } from "react";
import { useSelector } from "react-redux";

const PortfolioDetail = ({ spaceBottomClass, productFullDesc, productDetail }) => {


  return (
    <div className="">
      포트폴리오 상세화면 띄우기 및 모달
    </div>
  );
};

export default PortfolioDetail;
